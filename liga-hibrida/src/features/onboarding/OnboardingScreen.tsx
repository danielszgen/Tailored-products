import { ComingSoon } from '@/components/ComingSoon';

// PLACEHOLDER — replaced by the onboarding feature (SPEC §8.1).
export function OnboardingScreen() {
  return <ComingSoon title="Semana 0" stage="Etapa I" />;
}
