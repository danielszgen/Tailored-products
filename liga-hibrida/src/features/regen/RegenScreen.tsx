import { ComingSoon } from '@/components/ComingSoon';

// PLACEHOLDER — replaced by the REGEN feature (SPEC §8.6, Etapa I subset).
export function RegenScreen() {
  return <ComingSoon title="Regen" stage="Etapa I" />;
}
