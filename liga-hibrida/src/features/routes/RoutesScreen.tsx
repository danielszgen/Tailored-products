import { ComingSoon } from '@/components/ComingSoon';

// RUTAS and Zona Salvaje logging arrive with the rules engine (SPEC §9 Etapa II).
export function RoutesScreen() {
  return <ComingSoon title="Rutas" stage="Etapa II" />;
}
