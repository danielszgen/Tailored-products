import { ComingSoon } from '@/components/ComingSoon';

// PLACEHOLDER — replaced by the GYM combat feature (SPEC §8.3).
export function GymSessionScreen() {
  return <ComingSoon title="Combate" stage="Etapa I" />;
}
