import { ComingSoon } from '@/components/ComingSoon';

// PLACEHOLDER — replaced by the GYM feature (SPEC §8.3).
export function GymScreen() {
  return <ComingSoon title="Gimnasios" stage="Etapa I" />;
}
