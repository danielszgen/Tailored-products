import { ComingSoon } from '@/components/ComingSoon';

// PLACEHOLDER — replaced by the HOY feature (SPEC §8.2).
export function TodayScreen() {
  return <ComingSoon title="Hoy" stage="Etapa I" />;
}
