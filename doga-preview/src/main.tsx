import React from 'react';
import ReactDOM from 'react-dom/client';
import TailoredProductsPreview from './TailoredProductsPreview';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
	<React.StrictMode>
		<TailoredProductsPreview />
	</React.StrictMode>
);
